{
  "name": "api-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./build.zip",
    "zip": "zip -r build.zip .",
    "push": "aws s3api put-object --bucket pie-defi-lambdas --key v1.1.zip --body ./build.zip"
  }
}
